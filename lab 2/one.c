// if a five digit number is input.wap to print a new number by adding one to each of its digit

#include <stdio.h>

int main(){
    int a;
    int b;
    int c;
    int d;
    int e;

       printf("enter first number:");
         scanf("%d", &a);
       printf("enter second number:");
         scanf("%d", &b);
       printf("enter third number:");
         scanf("%d", &c);
       printf("enter fourth number:");
         scanf("%d", &d);
       printf("enter fifth number:");
         scanf("%d", &e);
    return 0;
    printf("the number is %d%d%d%d%d",a,b,c,d,e);
}